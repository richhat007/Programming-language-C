#include<stdio.h>
#include<stdlib.h>
int main(){
	int m,k,e,h,c,b,p;
	float average;
	int score;
	char grade;
	
	
	if(avg<95)
		if(avg<85)
			if(avg<75)
				if(avg<65)
					if(avg<50)
						g='F';
					else
						g='E';
				else
					g='D';
			else
				g='C';
		else
			g='B';
	else
		g='A';