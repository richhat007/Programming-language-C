	if(total>=500)
		if(total>=600)
			if(total>=700)
				if(total>=800)
					if(total>=900)
						if(total>=1000)
							discount=total * 0.9;
						else
							discount=total * 0.8;
					else
						discount=total * 0.7;
				else
					discount=total * 0.6;
			else
				dsicount=total * 0.5;
		else
			dsicount=total * 0.4;
	else
		dsicount=total * 0.3;
						